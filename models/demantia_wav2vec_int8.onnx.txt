Placeholder only.

No binary ONNX model is committed at this path. The committed runtime config
expects external Phase D wav2vec2 deployment assets under
`models/phaseD_frozen_ensemble_deploy/`. See `models/README.md` and
`configs/edge_inference.onnx.example.yaml` for the external-model setup.
